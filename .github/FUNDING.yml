# GitHub renders this as the repo's "Sponsor" button - the easy donation
# surface. Same destination as the in-app About support link (one source of
# truth would be nicer, but GitHub only reads this file).
github: Se7enbrc
